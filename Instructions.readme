1. throw it on a grid with a cockpit (set to main cockpit (optional)) and a turret of some kind
2. (optional) open customdata to configure the projectile velocity and hit recompile (default setup for gatlings)
3. run the pb with "automatic" as argument and it will toggle autoaim
